---
name: extra-points-filing
title: 学年综测附加分申报（多来源表检索 → 填表 → 逐条来源截图 → 分层打包交付）
description: 从本地一堆「附加分来源表」（xlsx/xls/docx/pdf，常为各学院、各社团互相流转的大表）中检索某一位学生的全部加分记录，按学校加分规则归类、去重、算总分，填进学校下发的《个人附加分申报表》模板，并为每一项内容生成对应的「来源表格截图」作为核对证据，最后打包成一个分层文件夹交付。触发词：附加分申报、综测附加分、综合测评、加分申报表、帮我填申报表、来源截图、标明出处并截图。
agent_created: true
---

# 学年综测附加分申报（全流程 SKILL）

> 本 SKILL 是**参考骨架**，目的是减少提示词填写、把流程精准化。
> 用户提供的材料与需求总有特殊性，必须按第 5 节的「即时交互点」随时和用户对齐，不要机械照搬。

技术子流程「把来源表的指定几行裁剪成 PNG 截图」已独立成技能 `excel-range-screenshot`
（`~/.workbuddy/skills/excel-range-screenshot/`），本技能负责调度它。

---

## 0. 开工前必做（先说清楚，再动手）

### 0.1 环境准备（提前告诉用户需要什么）
| 依赖 | 用途 | 说明 |
|---|---|---|
| Microsoft Excel | 表格裁剪、公式重算、导 PDF | 仅 Windows；macOS/Linux 见 `PORTABLE.md` |
| Python 3.10+ | 解析 xlsx/xls/docx、COM 自动化、光栅化 | **不要写死路径**，用 `$PYTHON` 占位，按机器实际解释器填 |
| 依赖包 | — | `pip install -r requirements.txt`（Windows+Excel 再 `pip install pywin32`） |

> 本技能不依赖任何 MCP 或 WorkBuddy 插件，只要本机有 Excel。离开 WorkBuddy 也能跑，见 `PORTABLE.md`。

一次性装依赖：
```bash
PYTHON=python        # 按机器实际填，例如 WorkBuddy 托管解释器的绝对路径
SKILL_DIR=~/.workbuddy/skills/extra-points-filing    # 本技能所在目录（scripts/ 的上一级）
"$PYTHON" -m pip install -r "$SKILL_DIR/requirements.txt"
# 没有 requirements.txt 时：openpyxl xlrd pillow pymupdf（Windows+Excel 再加 pywin32）
```

### 0.2 时间预估（开工前告知用户）
| 阶段 | 预估 |
|---|---|
| 全量检索（含几百~几千行的多张大表） | 2–5 分钟 |
| 归类 + 冲突确认（需要用户回答问题） | 1–3 分钟（取决于用户回复速度） |
| 填表 + 重算校验 | 1–2 分钟 |
| 逐条来源截图 | 每张 20–40 秒，一般 4–8 张 |
| **总计** | **约 8–15 分钟**（若来源表上万行或截图超过 10 张会更久） |

### 0.3 截图核对提醒（务必说）
因为交付物里有**截图**，务必提醒用户：**核对请用视觉大模型看图，文字描述替代不了**。
可推荐 LLM：**DS-V4.1-flash**。

### 0.4 可移植性（离开 WorkBuddy 还能用吗）
4 个脚本都是**纯 Python 命令行工具 + JSON 配置**，不 import 任何 WorkBuddy 的东西，拷走就能跑。
但平台能力有差别：检索/填表/自检跨平台；**来源截图、Excel 重算、页数检查只在 Windows + Excel 下可用**。
详见 `PORTABLE.md`（含平台矩阵、独立用法、macOS/Linux 替代方案）。

---

## 1. 身份与材料确认（不问清楚不许动笔）

**硬性必问，缺一不可：**
- 姓名（全名）
- 学号
- 班级（判定「全班都有的集体荣誉」时必需）
- 学院（判定「其他学院活动只认证学术/志愿类」时必需）
- 学年（例：2025-2026 学年）

**冲突处理**：若学号与姓名对不上（同一个学号挂着别的姓名，或同一姓名出现两个学号），
**立即停下问用户**，不要擅自选一个继续。

**来源表 vs 待填表 甄别**（不明确必须问）：
- 待填表：特征是**有「班级/姓名/学号」空格、各类别上限标注、小计与总分公式、签名栏**，且行数极少（<40 行）。
- 来源表：特征是**有「序号/院系/班级/学号/姓名/奖项/级别/个人集体/分值/备注」列头**，且行数成百上千。
- 一个文件夹里可能混着「别人的表」「空白模板」「说明文档」，不要把模板当来源表检索。

---

## 2. 全量检索（宁多勿漏，标签必须分档）

### 2.1 标签分三档（这是防假阳性的关键，实测有效）
| 档位 | 标签 | 命中后的处理 |
|---|---|---|
| **强标签** | 姓名全名、学号全号、`--extra` 给的别名 | 直接认定为本人记录 |
| **弱标签** | 姓名去姓/去名片段、学号后4位/后3位 | 只进「疑似命中」，**必须逐条人工排除** |
| **班级标签** | `--clazz` | 只进「候选集体条目」（全班都有的荣誉） |

**为什么不能把片段当强标签**：学号后 3 位 `123` 会撞上序号列 `123`、他人学号 `202511680123`；
实测把弱标签当强标签会多出 4 条假记录。名字片段（如 `张三`）也可能撞上同名近名。

**为什么班级写法极不统一**：同一张表里会出现 `24xx` / `24xx1` / `24xx1班` / `24专业全称1班`，
所以班级只能做「候选」信号，不能做「本人」信号。

### 2.2 一条命令跑完三层检索
```bash
PY="$PYTHON"        # $PYTHON 在 §0.1 定义；WorkBuddy 里是托管解释器，别的机器就是普通 python
"$PY" "$SKILL_DIR/scripts/scan_sources.py" \
      --root "<来源文件夹>" --name "张三" --sid "202400000001" \
      --clazz "24xx" --out hits.txt
```
脚本做三件事：
1. **openpyxl 逐 sheet 逐行**扫 `xlsx`（`xlrd` 扫 `.xls`）；
2. **docx 正文文本**扫描（说明、通知常是 docx）；
3. **XML 兜底**：直接在 `xl/sharedStrings.xml` 里 `count()` 各标签，用来发现
   「openpyxl 没扫到但文件里确实有」的情况（单元格被拆分、公式缓存、合并单元格）。

### 2.3 结果判读（A/B/C/D 四区）
- **A 个人命中** → 直接可申报的记录；
- **B 疑似命中** → 逐条排除（基本是序号列/他人学号巧合，但不能跳过这步）；
- **C 候选集体条目** → 「全班都有」的荣誉，按 3.3 判断是否折半；
- **D XML 兜底** → 某文件计数 > 0 但 A 区无命中 → 一定漏了，必须人工打开定位；
  计数 == A 区命中数 → 检索可信。

### 2.4 「查得到但归属不明」的条目也必须报给用户
- 只写**班级**没写学号的集体荣誉行；
- 只写**职务条件**的行（例：`班长 1.3 分`，表里没名字）；
- 同名/近名但学号不同的行；
- 用户放了某社团/某赛事的文件夹但检索不到 → 明确汇报「已检索、无记录」，并提示可找主办方开证明。

---

## 3. 归类与计算（规则清单 + 必须向用户确认的点）

### 3.1 通用上限（每所学校略有差异，以下为北语版，用前先核对学校的《综合素质测评办法》）
| 类别 | 上限 |
|---|---|
| 附加分总分 | 10 分 |
| 学术活动（专业学术） | 4 分 |
| 文体科技实践 | 3 分 |
| 荣誉表彰 | 3 分 |
| 志愿服务 | 2 分 |
| 其他文章 | 1 分 |
| 论文/艺术作品 | 按学校科研处刊物分类（特类/A/B/C/内部学术刊物），需附期刊原件 |

### 3.2 不累计规则（逐条对照，这是最容易错加分的地方）
1. 同类项目不同级别 → 只按最好成绩；
2. 同一比赛、同一体育赛事的不同奖项 → 只按最好成绩；
3. 体育赛事裁判、文体活动表演（含主持、播报）→ 每类每学年**最多加 2 次**，取最高；
4. **多个学生工作职务 → 只取最高一项**；
5. 比赛的「参与奖」不受上述限制；
6. 晚会表演、校运会表演 → 取最高的 2 项；
7. 献血 0.2 分/次，一学年上限 2 次，需献血证；
8. 参与其他学院的活动 → 本学院**只认证学术类和志愿类**（优秀宿舍按集体荣誉核算）；
9. **集体项目（优秀宿舍/先进班集体/优秀团支部）成员按对应分值 1/2 加分** ← 这条是高频冲突点，见 3.3。

### 3.3 必须向用户确认的高频歧义（先本地查证，查不到再问）
| 歧义 | 先自查 | 再问用户 |
|---|---|---|
| 集体荣誉是全额还是折半 | 查学院汇总表表头备注、班级群通知 | 两种口径都报给用户，让其选 |
| 同一荣誉在两张表里出现两次 | 对比项目名称、时间、负责部门 | 若判断为同一荣誉则只计一次 |
| 是否担任班委 | 查《班委职务加分用表》是否空白 | 问用户本人 |
| 学年内是否献血 | 无表可查 | 问用户本人 |
| 某加分项属哪个类别（如「校史馆讲解员」放在文体还是志愿） | 按来源表原分类 | 分类存疑时问用户 |
| 是否还有表外活动（论文、校外竞赛、社团） | 全文检索 | 问用户本人 |
| **是否有候选但未录入的记录**（如模联、校外赛） | 检索对应文件夹 | 检索不到要明确告知「已查、无记录」，并提示可找主办方开证明 |

**提问方式**：用 `AskUserQuestion` 一次问完（最多 4 题），选项里把两种口径的**分值后果**都写清楚
（例：「按班级通知不折半 → 荣誉小计按全额计」/「按学院 1/2 → 荣誉小计折半计」，并给出两种口径各自算出的总分）。

---

## 4. 冲突、补充与预输出确认

1. **信息冲突必须显式告知用户**（如上表的口径冲突），并在交付说明里留痕，方便班长核对。
2. **允许用户后续补充**：结构设计成「条目列表 → 分类映射 → 分值」三段，后续新增条目只往列表里加，
   不推翻已编码的结构。
3. **输出前必须让用户确认条目准确完备**：把「已录入条目 + 已排除事项 + 存疑事项」三张清单一起发给用户，
   明确问「就这些了吗？还有要补的吗？」。
4. **预输出完整性核对（用姓名+学号多标签反复核）**：
   - 每一条填进去的分值，都能在来源表里找到对应行号；
   - 每一条来源行号，都在交付截图里出现；
   - 总分 = 各类小计之和，且各类小计 ≤ 上限。

---

## 5. 生成成品（填表 → 截图 → 分层打包）

### 5.1 标准交付目录（所有产物打包成一个文件夹，内部分层）
```
<项目文件夹>/
├── <姓名>附加分申报表.xlsx            ← 主件（原模板重命名为 姓名附加分申报表）
├── <姓名>附加分申报表_预览.png         ← 一页预览图，方便快速查看
└── 附加分来源截图/                     ← 单独文件夹放证据
    ├── 00_截图与申报表条目对照说明.txt  ← 条目→分值→截图→来源文件/工作表/原行号
    ├── 01_<条目名>_<源表名>-<工作表>.png
    ├── 02_...
    └── ...
```

### 5.2 填表（`scripts/fill_template.py` + JSON 配置）
流程：**复制模板**（绝不在原模板上改）→ openpyxl 填值 → 写小计/总分公式 → 分值列格式 `0.000`
→ 打印区 `A1:G27` + 缩放到一页 → **Excel COM `CalculateFullRebuild()` 后 `Save()` 回写公式缓存值** →
导 PDF 数页数确认 1 页。

```bash
"$PY" "$SKILL_DIR/scripts/fill_template.py" --config form_config.json
```
配置要点见脚本头部注释。

### 5.3 逐条来源截图（`scripts/make_range_shots.py` + JSON 配置）
每张截图必须满足（这是「方便核对」的关键）：
- 保留原表的**表头行**与「活动性质」说明行，让人一眼看出这张表是什么性质；
- 目标行用**橙色底纹 + 红色边框**标注（源表标题行常自带黄色，用黄色会分不清谁是谁）；
- 底部追加一行**出处说明**：`来源：文件名 〉 工作表「xx」〉 原表第 n 行（橙色底纹＋红框标注）｜ 对应申报表条目`；
- 大表只保留表头 + 本人/本班相关行，其余行删除（**必须从下往上删**，否则行号漂移）。

```bash
"$PY" "$SKILL_DIR/scripts/make_range_shots.py" --config shots_config.json
```

### 5.4 对照说明（00 号文件）必须包含
1. 每个条目的 名称/级别/个人集体/分值/小计；
2. 每个条目对应的截图文件名 + 来源文件 + 工作表 + 原表行号；
3. 存疑口径及其处理方式；
4. **已核对并排除的事项清单**（无班委、无献血、无论文、某社团检索无记录等）——这能让班长少问很多问题；
5. 提交前用户本人要做的事（签名、装订、交给谁）。

---

## 6. 交付自检与收尾清理

### 6.1 自检（`scripts/final_check.py`）
- 申报表：姓名/学号/班级、各条目分值、各类小计、总分；
- 主体文件导 PDF 页数 == 1；
- 截图文件夹文件数、每张 PNG 可打开且尺寸合理；
- 条目 ↔ 截图对应关系逐条核对。

### 6.2 清理（**必做，不做会拖垮机器、污染交付包**）
- **残留 EXCEL.EXE**：pywin32 只 `xl.Quit()` **不会**结束进程，而且**每跑一次脚本就可能残留一个**
  （实测连续三轮各漏 1 个）。直接用自带的清理工具，它只杀「无可见窗口」的残留，不会误伤用户开着的 Excel：
  ```bash
  python "$SKILL_DIR/scripts/cleanup_excel.py" --dry-run   # 先看会杀谁
  python "$SKILL_DIR/scripts/cleanup_excel.py"             # 再动手
  ```
  原理：`EnumWindows + IsWindowVisible` 找出有可见顶层窗口的 Excel PID，这些**保留**；
  其余无窗口的才结束。手动判断也行：`Get-Process EXCEL | Select Id,MainWindowTitle`，标题为空即残留。
- **中间产物绝不写进交付包**：日志、临时 PDF、源表副本一律放 `tempfile.gettempdir()` 或 `.workbuddy/tmp`
  （项目级临时目录），否则交付包里会多出 `.workbuddy/`、`~$xxx.xlsx` 等杂质。
- 收工前 `ls` 一次交付包，确认只有该交付的东西。
- `.workbuddy/` 目录本身不要删（存项目记忆与工作数据）。

---

## 7. 本机踩坑速查

| 坑 | 现象 | 解决 |
|---|---|---|
| `Range.CopyPicture` 白图 | PNG 只有几百字节、内容全白 | Excel 不可见时必白图 → 改 `ExportAsFixedFormat` 导 PDF 再 PyMuPDF 转 PNG |
| 含中文的 `.ps1` 乱码 | 报「意外的标记」，行号指向离谱位置 | `.ps1` 必须存 **UTF-8 with BOM** |
| Python 沙箱拦截删除 | `shutil.rmtree` / `os.remove` 抛 safe-delete 异常 | 临时目录用带时间戳的唯一名，结束再逐个 `unlink`/`rmdir` |
| Bash 调 `powershell.exe` | 被安全策略拒绝 | 用 PowerShell 工具；且该工具**不回显 stdout**，结果必须写日志文件再 Read |
| 数字格式 `0.###` | 整数 1 显示成 `1.` | 用 `0.000` |
| openpyxl 写的公式无缓存 | 部分阅读器显示空白 | Excel COM 重算后 `Save()` 回写 |
| `.xls` 老格式 | openpyxl 报错 | 用 `xlrd` |
| 删行后行号漂移 | 标注错行 | 先算好各区间，**从下往上删**；新行号 = 原行号 − 上方被删行数 |
| 居中长文本被左右裁掉 | 说明行少字 | 未合并的居中单元格会两边同时裁 → 先合并 A:J 或保持左对齐，并 `WrapText` + 抬行高 |
| 班级/姓名写法不统一 | 检索漏 | 用片段标签 + XML 兜底 |
| 短片段当强标签 | 多出假记录（学号后3位 `123` 撞上序号列 `123`、他人学号 `202511680123`） | 标签必须分「强/弱/班级」三档，弱标签只进「疑似」区人工排除 |
| 导 PDF 报「无法保存该文档。该文档可能已被打开」 | 页数检查失败 | 文件正被别的 Excel 进程占用，或输出目录不可写 → `Workbooks.Open(path, ReadOnly=True)` + 临时 PDF 放**系统临时目录**；并把该检查做成可跳过，别让它挡住其它检查项 |
| 临时文件写进交付包 | 交付包里多出 `.workbuddy/`、`~$xxx.xlsx` 等杂质 | 中间产物一律写 `tempfile.gettempdir()`，日志路径做成可配置（`log_path` / `tmp_dir`） |
| COM 每跑一次就可能残留一个无窗口 Excel | 内存被慢慢吃光 | **每次**跑完都查一次：`Get-Process EXCEL`，`MainWindowTitle` 为空的才结束；别把有窗口的当成残留 |
| 用户中途移动/改名交付物 | 自检脚本报「文件不存在」 | 自检配置里的 `base_dir` 以**实际当前路径**为准，跑之前先 `os.listdir` 确认 |

---

## 8. 脚本与示例（均已用 2025-2026 学年北语国传院真实数据跑通）

| 文件 | 用途 |
|---|---|
| `scripts/scan_sources.py` | 多标签（强/弱/班级）全量检索 xlsx/xls/docx + XML 兜底校验，输出 A/B/C/D 四区 |
| `scripts/fill_template.py` | 按 JSON 配置填模板、写公式、设一页打印、COM 重算回写缓存 |
| `scripts/make_range_shots.py` | 裁剪来源表指定行 → 橙底红框标注 → 导 PDF → 转 PNG（通用参数化版） |
| `scripts/final_check.py` | 交付自检：数值 / 页数 / 类别上限 / 截图完整性 |
| `scripts/cleanup_excel.py` | 只结束「无可见窗口」的 Excel 残留进程，不误伤用户开着的 Excel（Windows） |
| `README.md` | 面向人的通俗使用手册：环境安装、逐步命令、配置字段讲解、FAQ、踩坑表 |
| `examples/form_config.example.json` | 填表配置真实样例 |
| `examples/shots_config.example.json` | 截图任务配置真实样例 |
| `examples/check_config.example.json` | 自检配置真实样例 |
| `requirements.txt` | 依赖清单（pywin32 仅 Windows） |
| `PORTABLE.md` | **离开 WorkBuddy 怎么用**：平台矩阵、独立用法、macOS/Linux 替代方案 |

**实测结果（样例数据）**：检索 A 区命中 3 条（与人工逐行核对完全一致）、B 区疑似 3 条（均为他人学号巧合，
人工排除）、C 区候选集体 256 条（含 2 条「全班都有」荣誉）；PDF 1 页；截图 7 个文件。

配套参考：`~/.workbuddy/skills/excel-range-screenshot/SKILL.md`（截图子流程的完整技术细节与更多坑）。

---

## 9. 即时交互点（什么时候必须停下来问用户）

1. 姓名与学号对不上；
2. 分不清哪张是来源表、哪张是要填的表；
3. 集体荣誉的折半口径有冲突；
4. 出现「只有条件语词（职务/班级）没有姓名学号」的分数行；
5. 某加分项不知道归哪个类别；
6. 用户提出要补充表外材料；
7. 任何会被写进正式提交材料的判断，只要有两种合理解释，就让用户选，并说明各自的分值后果。
